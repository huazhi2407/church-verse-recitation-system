rules_version = '2';

// 錄音檔：僅本人可上傳；已登入者可讀（供管理員確認錄音）
service firebase.storage {
  match /b/{bucket}/o {
    match /recordings/{userId}/{allPaths=**} {
      allow write: if request.auth != null && request.auth.uid == userId;
      allow read: if request.auth != null;
    }
  }
}
